# Hardening & Deploy (Phase 10) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Drawing Arena as one Render free-tier web service (server + built client), with a real README.

**Architecture:** Express serves `client/dist` statically with an SPA fallback; a committed `render.yaml` Blueprint defines the service; `MODEL_MANIFEST_PATH` env fixes the compiled-path manifest bug. Verified locally in prod mode before pushing.

**Tech Stack:** Express 5 static middleware, Render Blueprint (render.yaml), Playwright for the prod-mode smoke test.

**Spec:** `docs/superpowers/specs/2026-07-06-deploy-design.md`

## Global Constraints

- No new dependencies. The Render dashboard steps are the user's; everything else is code.
- Build must use `npm ci --include=dev` (Render sets NODE_ENV=production).
- Start command: `node server/dist/server/src/index.js` from the repo root.
- All paths relative to repo root.

---

### Task 1: Server serves the built client

**Files:**
- Modify: `server/src/index.ts:1-24`

- [ ] **Step 1: Add static serving + SPA fallback**

In `server/src/index.ts`, extend the node imports and add the static block after the `/health` route:

```ts
import { existsSync } from 'node:fs'
import { createServer } from 'node:http'
import path from 'node:path'
```

After the `app.get('/health', ...)` route:

```ts
// production: one service serves the built client too (same origin as the socket)
const clientDist = process.env.CLIENT_DIST ?? path.resolve('client/dist')
if (existsSync(path.join(clientDist, 'index.html'))) {
  app.use(express.static(clientDist))
  // SPA fallback for client routes (/play, /rooms/:code, …)
  app.use((req, res, next) => {
    if (req.method !== 'GET' || req.path.startsWith('/socket.io')) return next()
    res.sendFile(path.join(clientDist, 'index.html'))
  })
  console.log(`serving client from ${clientDist}`)
}
```

- [ ] **Step 2: Typecheck + unit tests**

From `server/`: `npm run typecheck` and `npm test` — green.

- [ ] **Step 3: Commit**

```bash
git add server/src/index.ts
git commit -m "Phase 10 task 1: serve the built client from the game server"
```

---

### Task 2: render.yaml + local prod-mode verification

**Files:**
- Create: `render.yaml`
- Temp (deleted after): `client/e2e/tmp-prod-smoke.e2e.mjs`

- [ ] **Step 1: Write `render.yaml`**

```yaml
# One free web service: the game server + the built client (same origin).
# Deploy: Render dashboard -> New + -> Blueprint -> select this repo -> Apply.
services:
  - type: web
    name: inkwars
    runtime: node
    plan: free
    healthCheckPath: /health
    buildCommand: npm ci --include=dev --prefix client && npm ci --include=dev --prefix server && npm run build --prefix client && npm run build --prefix server
    startCommand: node server/dist/server/src/index.js
    envVars:
      # prompts.ts resolves the manifest relative to the compiled file, which is wrong
      # under dist/ — point it at the built client copy instead (cwd-relative).
      - key: MODEL_MANIFEST_PATH
        value: client/dist/model/manifest.json
      - key: NODE_VERSION
        value: 22.16.0
```

- [ ] **Step 2: Build both packages locally**

From repo root: `npm run build --prefix client` then `npm run build --prefix server`.
Expected: `client/dist/index.html` and `server/dist/server/src/index.js` exist.

- [ ] **Step 3: Prod-mode smoke test with Playwright**

Start the compiled server from the repo root with `MODEL_MANIFEST_PATH=client/dist/model/manifest.json` and `PORT=3199`, then run a temp Playwright script against `http://localhost:3199` (no Vite): assert `/health` returns ok + 100 categories, the home page renders "Drawing Arena", `/play` loads the model and can start a match (model file served from dist), and a room can be created (socket works same-origin). Delete the temp script after.

- [ ] **Step 4: Commit**

```bash
git add render.yaml
git commit -m "Phase 10 task 2: render.yaml blueprint (single free web service)"
```

---

### Task 3: README rewrite

**Files:**
- Modify: `README.md` (full rewrite)

- [ ] **Step 1: Rewrite README** — sections: what it is (one paragraph + feature list: live AI
guesser in-browser, multiplayer rooms, chaos modifiers, replays, MACHINE commentary), tech
stack table, quick start (dev), production build + self-host, deploy on Render (Blueprint
steps), training your own model (pointer to `training/`), testing (unit + e2e commands),
project layout, live URL placeholder.

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "Phase 10 task 3: real README"
```

---

### Task 4: PROGRESS.md, push, user handoff

- [ ] **Step 1: Update PROGRESS.md** — check Phase 10 box (deploy pending user's Render
click-through), Phase 10 notes, final session log, "Next Immediate Step" = user's Render
steps + playtest.

- [ ] **Step 2: Commit and push**

```bash
git add PROGRESS.md
git commit -m "Phase 10: hardening/deploy prep complete"
git push origin master
```

- [ ] **Step 3: Hand the user their checklist** (Render Blueprint steps per spec). After they
paste the live URL, verify it end-to-end in a browser (home, /play round, room creation) and
fill the README's live-URL placeholder in a final commit.
